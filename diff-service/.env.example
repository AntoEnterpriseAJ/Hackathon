# Which implementation to use for each layer
# Change these to swap out components without touching code
EXTRACTOR=pdfplumber
PARSER=fd
DIFFER=difflib
ANALYZER=regex

# Flask
FLASK_PORT=5000
FLASK_DEBUG=true

# CORS — allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:4200,http://localhost:4000
